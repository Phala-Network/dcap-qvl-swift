// This package is populated by the dcap-qvl release pipeline.
// The real UniFFI-generated `DcapQvl.swift` and the matching
// `DcapQvlFFI.xcframework` URL + checksum are written by
// `.github/workflows/ios-release.yml` in Phala-Network/dcap-qvl on each
// `v*` tag. Until the first release, this package has no versioned tags
// and cannot be resolved by SwiftPM — that is expected.
